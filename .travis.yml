language: node_js
sudo: required
dist: trusty
addons:
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
node_js:
- '8.9.4'
install:
  - yarn
script:
  - yarn run build
