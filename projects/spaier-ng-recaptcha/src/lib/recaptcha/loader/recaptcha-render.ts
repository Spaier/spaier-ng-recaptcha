/**
 * A predefined render parameter values.
 */
export enum RecaptchaRender {
  /**
   * Nothing is rendered.
   */
  Explicit = 'explicit',
  /**
   * First element with g-recaptcha class is rendered.
   */
  Onload = 'onload',
}
