/**
 * Parameters for the grecaptcha.execute function.
 */
export interface RecaptchaExecuteParameters {
  /**
   * Specifies V3 recaptcha's action parameter.
   * Attribute: data-action.
   */
  action?: string
}
