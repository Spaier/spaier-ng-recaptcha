import { InjectionToken } from '@angular/core'

/**
 * If set to true, enables rendering with g-recaptcha class.
 */
export const RECAPTCHA_IMPLICIT = new InjectionToken<boolean>('RECAPTCHA_IMPLICIT')
